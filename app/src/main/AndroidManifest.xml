<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.miracostacollege.mcc_officehoursapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".updateScheduleActivity"></activity>
        <activity android:name=".ViewScheduleActivity" />
        <activity android:name=".UpdateScheduleActivity" />
        <activity android:name=".ProfessorLoggedInView" />
        <activity android:name=".TeacherRegisterActivity" />
        <activity android:name=".AnonymousStudentSearch" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".Login" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>